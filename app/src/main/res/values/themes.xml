<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Custom application theme based on MaterialComponents DayNight with no ActionBar -->
    <style name="Theme.MyTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Define primary colors for the theme -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <!-- Custom shape appearance for a small square Floating Action Button -->
    <style name="ShapeAppearance.App.SmallComponent.SquareFAB" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Custom Floating Action Button widget that applies the above shape overlay -->
    <style name="Widget.App.FloatingActionButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.App.SmallComponent.SquareFAB
        </item>
    </style>

</resources>
